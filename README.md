# ModelMix-Text-Generator
Python script that compares text generation across multiple language models. Accepts user input, generates responses using open-source and paid models, tests three temperature values, and saves all outputs in structured JSON for easy comparison and analysis.
